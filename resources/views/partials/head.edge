<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ pageTitle || 'Biblioteca Local' }}</title>
    {{ csrfMeta() }}
    @vite(['resources/js/app.ts'])
    @stack('dumper')
  </head>
