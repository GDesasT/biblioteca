@let(pageTitle = 'Usuarios')
@include('partials/head')
@include('partials/page_start')

<section class="mb-5 flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
  <div>
    <p class="text-sm font-bold text-slate-500">Directorio</p>
    <h2 class="text-2xl font-black">Usuarios registrados</h2>
  </div>
  @if(authUser.role.name === 'Administrador')
    <a href="/users/create" class="focus-ring inline-flex rounded-md bg-teal-600 px-4 py-2.5 text-sm font-black text-white hover:bg-teal-700">Nuevo usuario</a>
  @endif
</section>

<form method="GET" action="/users" class="mb-5 grid gap-3 rounded-md border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-800 dark:bg-slate-950 md:grid-cols-[1fr_240px_auto]">
  <input name="search" value="{{ filters.search }}" class="focus-ring rounded-md border border-slate-300 px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900" placeholder="Nombre, email o numero..." />
  <select name="roleId" class="focus-ring rounded-md border border-slate-300 px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900">
    <option value="">Todos los roles</option>
    @each((role) in roles)
      <option value="{{ role.id }}" {{ String(role.id) === String(filters.roleId) ? 'selected' : '' }}>{{ role.name }}</option>
    @endeach
  </select>
  <button class="focus-ring rounded-md bg-slate-900 px-4 py-2 text-sm font-black text-white dark:bg-white dark:text-slate-950">Filtrar</button>
</form>

<section class="rounded-md border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-950">
  <div class="overflow-x-auto">
    <table class="w-full min-w-[820px] text-left">
      <thead class="bg-slate-50 text-xs font-black uppercase text-slate-500 dark:bg-slate-900 dark:text-slate-400">
        <tr>
          <th class="table-cell">Usuario</th>
          <th class="table-cell">Numero</th>
          <th class="table-cell">Rol</th>
          <th class="table-cell">Alta</th>
          <th class="table-cell text-right">Acciones</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-slate-100 dark:divide-slate-800">
        @each((userRecord) in users)
          <tr>
            <td class="table-cell">
              <p class="font-bold">{{ userRecord.fullName }}</p>
              <p class="text-xs text-slate-500">{{ userRecord.email }}</p>
            </td>
            <td class="table-cell font-bold">{{ userRecord.userNumber }}</td>
            <td class="table-cell"><span class="badge bg-teal-50 text-teal-700 dark:bg-teal-500/15 dark:text-teal-200">{{ userRecord.role.name }}</span></td>
            <td class="table-cell text-slate-500">{{ userRecord.createdAt.toFormat('dd/LL/yyyy') }}</td>
            <td class="table-cell">
              @if(authUser.role.name === 'Administrador')
                <div class="flex justify-end gap-2">
                  <a href="/users/{{ userRecord.id }}/edit" class="rounded-md border border-slate-300 px-3 py-2 text-sm font-bold dark:border-slate-700">Editar</a>
                  <form method="POST" action="/users/{{ userRecord.id }}">
                    {{ csrfField() }}
                    <input type="hidden" name="_method" value="DELETE" />
                    <button class="rounded-md border border-rose-200 px-3 py-2 text-sm font-bold text-rose-700 hover:bg-rose-50 dark:border-rose-900">Eliminar</button>
                  </form>
                </div>
              @else
                <span class="block text-right text-sm text-slate-400">Consulta</span>
              @endif
            </td>
          </tr>
        @endeach
      </tbody>
    </table>
  </div>
</section>

<div class="mt-6 flex items-center justify-between rounded-md border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-950">
  <a href="{{ users.getPreviousPageUrl() || '#' }}" class="rounded-md px-3 py-2 text-sm font-bold {{ users.getPreviousPageUrl() ? 'text-slate-700 hover:bg-slate-100 dark:text-slate-200 dark:hover:bg-slate-900' : 'pointer-events-none text-slate-400' }}">Anterior</a>
  <span class="text-sm font-bold text-slate-500">Pagina {{ users.currentPage }} de {{ users.lastPage }}</span>
  <a href="{{ users.getNextPageUrl() || '#' }}" class="rounded-md px-3 py-2 text-sm font-bold {{ users.getNextPageUrl() ? 'text-slate-700 hover:bg-slate-100 dark:text-slate-200 dark:hover:bg-slate-900' : 'pointer-events-none text-slate-400' }}">Siguiente</a>
</div>

@include('partials/page_end')
