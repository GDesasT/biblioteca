@let(pageTitle = 'Iniciar sesion')
@include('partials/head')
<body class="min-h-screen bg-slate-950 text-white">
  <main class="grid min-h-screen lg:grid-cols-[1fr_520px]">
    <section class="relative hidden overflow-hidden lg:block">
      <div class="absolute inset-0 bg-[linear-gradient(135deg,#0f766e_0%,#1d4ed8_52%,#7c3aed_100%)]"></div>
      <div class="absolute inset-0 opacity-20" style="background-image: linear-gradient(#ffffff 1px, transparent 1px), linear-gradient(90deg, #ffffff 1px, transparent 1px); background-size: 48px 48px;"></div>
      <div class="relative flex h-full flex-col justify-between p-12">
        <div class="flex items-center gap-3">
          <div class="grid h-12 w-12 place-items-center rounded-md bg-white text-xl font-black text-teal-700">B</div>
          <div>
            <p class="text-sm font-black uppercase">Biblioteca Local</p>
            <p class="text-sm text-white/75">AdonisJS 6 + MySQL</p>
          </div>
        </div>
        <div class="max-w-2xl">
          <p class="mb-5 text-sm font-bold uppercase text-teal-100">Administracion escolar y universitaria</p>
          <h1 class="text-5xl font-black leading-tight">Gestiona catalogo, prestamos, inventario y reportes sin salir de tu red local.</h1>
        </div>
      </div>
    </section>

    <section class="flex items-center justify-center bg-slate-50 px-5 py-10 text-slate-950">
      <div class="w-full max-w-md">
        <div class="mb-8 lg:hidden">
          <div class="grid h-12 w-12 place-items-center rounded-md bg-teal-600 text-xl font-black text-white">B</div>
        </div>

        <div class="mb-8">
          <p class="text-sm font-bold uppercase text-teal-700">Acceso local</p>
          <h2 class="mt-2 text-3xl font-black">Iniciar sesion</h2>
        </div>

        @include('partials/flash')

        <form method="POST" action="/login" class="space-y-5 rounded-md border border-slate-200 bg-white p-6 shadow-sm">
          {{ csrfField() }}
          <label class="block">
            <span class="text-sm font-bold text-slate-700">Email</span>
            <input name="email" type="email" required class="focus-ring mt-2 w-full rounded-md border border-slate-300 px-3 py-2.5 text-sm" placeholder="admin@biblioteca.local" />
          </label>

          <label class="block">
            <span class="text-sm font-bold text-slate-700">Password</span>
            <input name="password" type="password" required class="focus-ring mt-2 w-full rounded-md border border-slate-300 px-3 py-2.5 text-sm" placeholder="password123" />
          </label>

          <button class="focus-ring w-full rounded-md bg-teal-600 px-4 py-3 text-sm font-black text-white hover:bg-teal-700">
            Entrar
          </button>
        </form>

        <div class="mt-5 rounded-md bg-slate-100 p-4 text-sm text-slate-600">
          <p><strong>Admin:</strong> admin@biblioteca.local</p>
          <p><strong>Empleado:</strong> empleado1@biblioteca.local</p>
          <p><strong>Estudiante:</strong> estudiante1@biblioteca.local</p>
          <p><strong>Password:</strong> password123</p>
        </div>
      </div>
    </section>
  </main>
</body>
</html>
